import Logo from '../logo';
import NavMain from '../nav-main';

function Header() {
  return (
    <header className='header'>
      <Logo />
      <NavMain />
    </header>
  );
}

export default Header;
